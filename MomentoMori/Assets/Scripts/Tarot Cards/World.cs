using UnityEngine;

public class World : TarotMain
{
    // Identifies hidden items/collectables (Wont be active in this version)
    public void RevealItems()
    {
        
    }
    void Start()
    {
        Activate();
        RevealItems();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
