using UnityEngine;

public class WheelOfFortune : TarotMain
{
    // Players get given a random item at the start of each run (not included in this version)
    public void GiveItem()
    {
        
    }
    void Start()
    {
        Activate();
        GiveItem();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
